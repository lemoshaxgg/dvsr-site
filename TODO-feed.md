# TODO — товарный фид для Яндекс.Вебмастер → Товары

Фид сгенерирован и **закоммичен локально**, но **НЕ задеплоен и никуда не загружен**.
Продолжить позже по списку ниже.

## Где что лежит
- **Готовый фид:** `public/yandex-products-feed.yml` — 52 позиции (Септики 12 / Сваи 10 / Нержавейка 5 / Электрика 14 / Сигнал 11)
- **Генератор:** `scripts/gen-yandex-products-feed.cjs` → пересобрать: `node scripts/gen-yandex-products-feed.cjs`
- Наценка от закупки по порогам: ≤30 000 → +25%, 30 000–150 000 → +18%, >150 000 → +12%
- URL всех офферов — существующие ЧПУ `/catalog/{id}`, новые страницы создавать НЕ надо

## Осталось сделать
1. **Сверить цены по септикам** — проверить, что не встаём заметно дороже ПримСтройХаба.
   Текущая выборка — ходовые Топас 10–12 Long (закупка 290–315к → +12% → 330–352к).
   Промышленные ТОПАЭРО (~1.4 млн) намеренно исключены из фида.
2. **Задеплоить фид** — `git push` (Timeweb сам пересоберёт), проверить что `https://dsr-dv.ru/yandex-products-feed.yml` отдаётся.
3. **Загрузить в Яндекс.Вебмастер → Товары** («Предложения магазинов») — указать ссылку на фид.
4. **(Опционально) Добавить в Яндекс.Директ** — тот же фид как источник для товарной кампании.
5. **Добить нержавейку** — сейчас только 5 годных позиций (цена + фото). Нужна закупка + фото по направлению, потом расширить выборку в генераторе.

## Заметки
- Отсеяно 42 позиции без валидного фото (в основном дорогая электрика: щиты ЩМП, пускатели КМ, автоматы ВА88/ВА89, счётчик Меркурий). При появлении фото — попадут в фид автоматически.
- Фид проходит валидацию YML (как Фарпост-прайс), без телефона и лишних тегов в `<shop>`.
